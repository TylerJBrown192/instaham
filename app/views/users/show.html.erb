<h1> <%= @user.username %> </h1>

<br>
<%= image_tag @user.avatar.url(:thumb) %>

<br>
<Br>
<h2> Add a new picture: </h2>
<%= form_for @picture, :url => user_path, :html => { :multipart => true } do |form| %>
  <%= form.file_field :image %>
  <%= form.submit "Add Photo" %>
<% end %>
